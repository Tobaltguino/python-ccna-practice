services:
  - type: web
    name: examen-cisco-flet
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python examen_gui_flet.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
